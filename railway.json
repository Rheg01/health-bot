{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y openjdk-21-jdk git && git clone https://github.com/AsamK/signal-cli.git && cd signal-cli && ./gradlew build"
  }
}
